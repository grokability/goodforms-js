
//var Goodforms = require('./prod-verify.js')
//var Goodforms = require('./index.js'); //kinda works, but throws some errors.

var GF2 = require('./dist/prod-verify.js'); // fails, because there's no such thing as 'window'
console.log("Ehllo!");