import log from "./logging";


//var dohjs = require('dohjs')

export default {
    "verify": function (data, callback) {
        console.warn("Hey, we validated something! Isn't that nice :)")
        // do something.
        setTimeout(function () {
            data.email
        })
    }
}